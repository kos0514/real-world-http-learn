# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor directories and files
.idea

# TLS lab (ch07/02_tls) — keep dirs with .gitkeep, ignore secrets/state

# Ignore contents of any 'private' directory, but keep its .gitkeep
ch07/02_tls/**/private/*
!ch07/02_tls/**/private/.gitkeep


# Ignore OpenSSL serial files
ch07/02_tls/**/*.srl

# Optional: ignore CSRs and certs (uncomment if needed)
# ch07/02_tls/**/*.csr
# ch07/02_tls/**/*.crt